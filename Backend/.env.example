# ============================================================
# CONFIGURACIÓN DE BASE DE DATOS POSTGRESQL
# ============================================================
# 
# Para usar este archivo:
# 1. Copia este archivo: cp .env.example .env
# 2. Edita .env con tus credenciales reales
# 3. NUNCA subas el archivo .env a Git (ya está en .gitignore)
#
# Formato: postgresql+asyncpg://[USER]:[PASSWORD]@[HOST]:[PORT]/[DATABASE]
#
# Ejemplo para desarrollo local:
DATABASE_URL=postgresql+asyncpg://barrita:12345@127.0.0.1:5432/SistemaPrueba
#
# Ejemplo para Docker (usando nombre del servicio como host):
# DATABASE_URL=postgresql+asyncpg://barrita:12345@postgres:5432/SistemaPrueba

# ============================================================
# CONFIGURACIÓN DE EMAIL (SMTP)
# ============================================================
# Para Gmail:
#   1. Activa la verificación en 2 pasos en tu cuenta de Google
#   2. Ve a: https://myaccount.google.com/apppasswords
#   3. Crea una "contraseña de aplicación" y úsala aquí
#   4. Usa EMAIL_PORT=587 y EMAIL_HOST=smtp.gmail.com
#
# Para Outlook/Hotmail:
#   EMAIL_HOST=smtp-mail.outlook.com
#   EMAIL_PORT=587
#
# Para otros proveedores, consulta su documentación SMTP
# ============================================================
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=tu_email@gmail.com
EMAIL_PASSWORD=tu_app_password_aqui

# ============================================================
# CONFIGURACIÓN DE JWT (Seguridad)
# ============================================================
# SECRET_KEY: Clave para firmar tokens JWT
# - En producción, genera una clave segura: openssl rand -hex 32
# - NUNCA uses esta clave de ejemplo en producción
SECRET_KEY=a3f8ddkgidmd83a6e9d3c8b5f2a7e4d1c9b6f3a8e5d67dgr32c7f4a1e8d5b2c9mlci981e1

# Algoritmo de encriptación para JWT
ALGORITHM=HS256

# Tiempo de expiración del token de acceso (en minutos)
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Tiempo de expiración del token de refresco (en días)
REFRESH_TOKEN_EXPIRE_DAYS=7

